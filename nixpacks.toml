[phases.setup]
nixPkgs = ["nodejs", "python3"]

[phases.install]
cmds = ["python3 -c \"import subprocess,sys; subprocess.check_call([sys.executable,'-m','ensurepip','--upgrade'])\" 2>/dev/null; $(python3 -c \"import sys;print(sys.executable)\") -m pip install openpyxl"]
