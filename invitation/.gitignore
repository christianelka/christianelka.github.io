# AI / agent workflow state (not source)
.sisyphus/

# Browser automation test artifacts
.playwright-mcp/
playwright-report/
test-results/

# Archived legacy files (kept for reference, not deployed)
archive/

# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Lockfiles (not needed for static HTML)
package-lock.json
yarn.lock
pnpm-lock.yaml

# Env files
.env
.env.local
.env.*.local

# Logs
*.log

# Backup variants
*_backup.*
*_bak.*
* copy.*
*_copy.*
*.bak
*.backup
*_old.*

# OS files
Thumbs.db
.DS_Store
Desktop.ini
*.swp
*.swo
*~

# IDE
.vscode/settings.json
.idea/
*.sublime-project
*.sublime-workspace
